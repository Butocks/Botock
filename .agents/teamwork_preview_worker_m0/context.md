# Milestone 0 Context: Dependency Installation & next.config.ts

Target:
1. Install client-side libraries in `frontend`:
   `browser-image-compression`, `pica`, `@types/pica`, `@imgly/background-removal`, `onnxruntime-web`.
2. Configure scoped COOP/COEP headers in `frontend/next.config.ts` for `/tools/image-remove-bg`.
3. Verify baseline build: `npm run build` passes with exit code 0.
4. Write handoff report to `.agents/teamwork_preview_worker_m0/handoff.md`.
